# Word Universe SAT L7 Pilot - Draft Import

Date: 2026-07-01

## Summary

Created a SAT level 7 word-universe pilot as draft-only content.

Stage:

`wu-sat-l7-pilot-2026-07-01`

Scope:

- `def_to_word`: 30
- `synonym_choice`: 30
- `confusable_choice`: 30
- total: 90

All content is Claude-authored original content. No DeepSeek or external generation API was used.

## Changed Files

- `data/generated-question-sets/wu-sat-l7-pilot-2026-07-01/word-universe.json`
- `reports/word-universe-sat-l7-pilot-2026-07-01.md`

Validator/report JSON files were regenerated by required commands.

## Database Writes

Yes, draft-only.

| table | rows | status |
|---|---:|---|
| `question_sets` | 90 | draft |
| `question_items` | 90 | draft |
| `question_target_words` | 90 | n/a |

No active rows were written or promoted.

Global active qbank count remained unchanged at `2254`.

## Import Results

Dry-run:

```text
import-wu DRY-RUN: ok 90 · reject 0
  def_to_word: 30
  synonym_choice: 30
  confusable_choice: 30
```

Apply:

- first apply command timed out client-side at 124s;
- immediate DB inspection showed partial draft import: `79 sets / 79 items / 79 target_words`;
- idempotent apply rerun completed successfully and reported `dup-skip 90`;
- final DB inspection confirmed complete import: `90 sets / 90 items / 90 target_words`.

The timeout did not leave active rows or malformed rows.

## Repair During Self-Review

During semantic self-review, one draft item was found to have a double-answer risk:

- `stationary` prompt: "静止的，不移动的"
- old distractor: `stagnant`
- issue: `stagnant` can also mean not moving/flowing, so it could satisfy the prompt.
- fix: replaced `stagnant` with `statutory`.

The row was verified as:

- stage = `wu-sat-l7-pilot-2026-07-01`
- task = `confusable_choice`
- set status = draft
- item status = draft

Only that item choice text was updated. No broad deletion or task-wide operation was performed.

## Final DB State

Source-to-DB verification:

```json
{
  "stage": "wu-sat-l7-pilot-2026-07-01",
  "source": 90,
  "dbSets": 90,
  "dbItems": 90,
  "dbTargets": 90,
  "errors": 0
}
```

Detailed DB inspection:

```json
{
  "sets": 90,
  "items": 90,
  "targetWords": 90,
  "statuses": { "draft": 90 },
  "types": {
    "def_to_word": 30,
    "synonym_choice": 30,
    "confusable_choice": 30
  },
  "itemStatuses": { "draft": 90 },
  "wordIdNull": 0,
  "badCount": 0
}
```

## Answer Position Rebalance

Initial position distribution after import:

```text
def_to_word:       A30/B0/C0/D0
synonym_choice:    A30/B0/C0/D0
confusable_choice: A30/B0/C0/D0
```

Applied:

```powershell
npx tsx scripts/rebalance-wu-choices.ts --stage=wu-sat-l7-pilot-2026-07-01 --status=draft --apply
```

Result:

```text
rows 90 · changed 88
def_to_word:       A8/B8/C7/D7
synonym_choice:    A8/B8/C7/D7
confusable_choice: A8/B8/C7/D7
```

Idempotency proof:

```text
rebalance DRY-RUN ... rows 90 · changed 0
```

Answer position guard:

```text
draft|confusable_choice: A8/B8/C7/D7 (n=30, max 27%)
draft|def_to_word:       A8/B8/C7/D7 (n=30, max 27%)
draft|synonym_choice:    A8/B8/C7/D7 (n=30, max 27%)
```

## Quality Self-Review

Content was reviewed for:

- target word in dictionary (`wordIdNull=0`);
- answer appears in choices;
- four distinct choices;
- `def_to_word` and `confusable_choice` answer text equals target word;
- no deprecated task type;
- no active status;
- SAT L7 difficulty intent.

One issue was found and fixed: `stationary` vs `stagnant` double-answer risk.

No remaining P1 content issue is known in this pilot.

Some SAT word-universe choices remain content-review candidates before any future promotion, especially broader synonym choices such as `eclectic -> diverse` and `vindicate -> justify`. They are safe as draft and should receive a fresh promote-time review.

## Verification Commands

All completed with exit code 0:

```powershell
npx tsx scripts/import-authored-word-universe.ts --file=data/generated-question-sets/wu-sat-l7-pilot-2026-07-01/word-universe.json
npx tsx scripts/import-authored-word-universe.ts --file=data/generated-question-sets/wu-sat-l7-pilot-2026-07-01/word-universe.json --apply
npx tsx scripts/rebalance-wu-choices.ts --stage=wu-sat-l7-pilot-2026-07-01 --status=draft --apply
npx tsx scripts/rebalance-wu-choices.ts --stage=wu-sat-l7-pilot-2026-07-01 --status=draft
npx tsx scripts/audit-answer-position.ts --stage=wu-sat-l7-pilot-2026-07-01 --status=draft
npm run validate:qbank-v2
npm run qa:qsets-v2
npm run validate:practice-session
npm run validate:data-quality
npm run audit:wu-source
npm run validate:wu-promote-guards
npm run smoke:active-serve
npm run lint
npx tsc --noEmit
```

Note: the first `--apply` invocation timed out client-side but the operation was recoverable and final DB state is complete and verified. The successful idempotent rerun is the authoritative final apply verification.

## Active / Deprecated Counts

- active sets remained `2254`
- active word-universe source audit:
  - gen: 261
  - qb: 0
  - migrated_v1: 0
  - badProvenance: 0
- `antonym_choice`: not introduced
- `cet_cloze`: not introduced

## Frontend Changes

None.

## Open Risks

- This is a draft-only pilot. No SAT word-universe pilot item should be promoted before a separate promote manifest and manual content review.
- Some synonym choices are intentionally test-like but should be re-reviewed before active promotion.
- The broader audio/TOEFL/IELTS blocked work remains unchanged.

## Recommended Next Phase

Proceed to a small SAT L7 promote review phase only if the user wants SAT word-universe active coverage:

1. review all 90 pilot rows;
2. exclude any loose synonym or ambiguous confusable item;
3. promote at most 20 per type;
4. run active answer-position guard and full regression.

If the goal is exam coverage rather than word-universe coverage, the next major phase is TOEFL missing listening/reading/speaking content, but that requires external format/audio decisions.
