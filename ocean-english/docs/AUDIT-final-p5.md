# 最终整合包 · 阶段 5 用户友好审计报告

> 2026-06-11 · 分支 final-p2-vocab（阶段 1-5 连续提交）

## 1. 状态色唯一来源（STATE_META）

| 表面 | 来源 | 结论 |
|---|---|---|
| 列表 chip / 闪卡 / 热力图（浅色面） | `STATE_META.light` 直接引用 | ✅ 既有 |
| 词图节点/面板（暗色面） | 本次改为 `STATE_COLOR_DARK`（删除原型自带色板） | ✅ 已修 |
| 宇宙 v2 注入层（静态 JS） | 常量与 `STATE_META.dark` 逐色同步，文件内注明同步义务 | ✅ 已修（静态文件无法 import，记轻偏离） |
| 个别屏幕硬编码 hex（如 `#d4477e`） | grep 核对：值与 `STATE_META.light` 完全一致，无视觉分歧 | ⚠ 建议后续机械替换为引用（不影响一致性验收） |
| 记忆图谱五色（红/黄/绿/teal/灰） | 「记忆强度」语义（设计规格§三），非词状态色，独立定义 | ✅ 按规格 |

## 2. 返回完整性
- 宇宙：面包屑（宇宙›星系›词）逐级可点 / 选词 pushState、浏览器返回逐级退 / Esc 关词→退星系 ✅
- 词图：← 按钮 / 词迹 chips 任意点回 / Esc（对比卡→预警→上一词 分层）✅
- 聚焦页悬浮返回（FocusBackButton）既有 ✅

## 3. 双语完整性
- 宇宙星球面板：真实中文释义行 + 例句中英（词典 API + normalizeWord 适配）✅
- 词图 tooltip + 面板 + 对比卡：词/中文/音标 ✅
- 错题卡：题面中文 + 中文解析 ✅
- 偏离：宇宙静态星系页无「左侧词列表」（该列表属旧 React Shell；静态定稿版本无此结构），双语在面板与 tooltip 覆盖

## 4. 空状态给动作
- 记忆图谱 <10 词：「先去学今天的词包 → 去学习」✅
- 词图无关系：扇区自动隐藏（原型空扇区处理），面板显示「—」✅
- 巡航无今日词：不触发巡航（无变化不打扰）✅
- 宇宙无学习词：星系进度环不渲染、面板仍可浏览 + 加入学习 ✅

## 5. 首访引导（dismissible 单句 + localStorage flag）
- 词图：`lexigraph-v2-intro-seen` ✅（覆盖语义/记忆两模式说明）
- 宇宙：`lv2-intro-seen` ✅
 
## 6. reduced-motion 降级
- 宇宙巡航：直接定格全景 + 收尾文案 ✅
- 词图补间/渐进绘制：跳过缓动直接定格 ✅
- due 红点脉冲动画：media query 关闭 ✅

## 7. 闭环 E2E（Playwright 12/12 绿）
1. 定级(直选 L3)→今日包(level=3)→学(翻面+认识)→练(键盘答题)→celebrate 巡航 overlay→次日到期队列 ✅
2. 宇宙就地评分 → SRS interval 3→8、nextReviewAt 入未来（/memory 队列减少；记忆图谱颜色为纯函数派生）✅
3. 测验双错×2 → 记忆图谱红边(data-wrong-edges=1) → 辨析题双对 → 红边消退(=0) ✅
4. 词详情↔词图↔宇宙 三向带词跳转（含 iframe dock）✅

## 记录在案的偏离
- LexiVault 词脊路由映射（A6）：bundle 为 17MB 单文件成品（逐字节不变约束），可改的 lv-data.jsx 源文件未随交付包提供 → 无法实施
- >400 词/星系聚合星团渲染：静态星池固定 ~250 词无法触发；数据层（maxPlanets/ring catalog）已就位，渲染分支待真实词量接入星池后落地
- AI 关系层（阶段2-3B）：脚本骨架+断点缓存已建，因消耗 LLM 配额默认不执行，待用户确认后跑 `npx tsx scripts/generate-relations.ts --ai --levels 3,4`
