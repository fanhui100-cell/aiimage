# frontend/.env.production.example
# Copy this file as .env.production (for self-hosted) or set these in Vercel Dashboard

# Backend API URL — must be HTTPS in production
NEXT_PUBLIC_API_URL=https://api.yourdomain.com
